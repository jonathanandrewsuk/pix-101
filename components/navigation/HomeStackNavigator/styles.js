import { StyleSheet } from 'react-native';
import { COLOR_WHITE, COLOR_PRIMARY } from '../../../styles/common';


export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: COLOR_WHITE,
    alignItems: 'center',
    justifyContent: 'center',
  },
  headerStyle: {
    backgroundColor: COLOR_PRIMARY,
  },
});
